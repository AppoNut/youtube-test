platform :ios, '12.4'

inhibit_all_warnings!

target 'youtube_test' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod "youtube-ios-player-helper"
  pod "FloatingPanel"
end
